#!/usr/bin/python3

def best_score(a_dictionary):
""" returns a key with the biggest integer value """

def best_score(a_dictionary):
if not a_dictionary:
return (None)
"""else, if its more then 0:"""
return (max(a_dictionary, key=a_dictionary.get))
